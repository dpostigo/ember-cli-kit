import { makeAction } from '../../../helpers/make-action';
import { module, test } from 'qunit';

module('Unit | Helper | make action');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = makeAction(42);
  assert.ok(result);
});
