import { forContent } from '../../../helpers/for-content';
import { module, test } from 'qunit';

module('Unit | Helper | for content');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = forContent(42);
  assert.ok(result);
});
