import { bindAction } from '../../../helpers/bind-action';
import { module, test } from 'qunit';

module('Unit | Helper | bind action');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = bindAction(42);
  assert.ok(result);
});
